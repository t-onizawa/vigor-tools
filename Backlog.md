# VIGOR TOOLS — Backlog.md

```
Status: Living backlog（固定ロードマップではない）
Created: 2026-07-14
```

これは計画表ではない。優先度は仮説であり、公開後の反応で入れ替わる前提のバックログである。

VIGOR LAB は「計画を守る」より「小さく出して、数字を見て方向修正する」ことを優先する（DCAp思想）。
このドキュメントも同じ考え方で運用する。

判断基準は `VISION.md` に準拠する。特に Tool Principles・新ツール提案時の評価（5軸）を参照。

VIGOR LAB全体（TOOLS・MATSURI・note横断）の中長期ロードマップ・収益設計・
Founderレビューは `Roadmap.md` を参照。本書は日々の意思決定ログを持つ。

---

## 25本フェーズの方針（2026-07-15確定）

VIGOR TOOLSは「広く作って、反応で深める」方針で運営する。

```
13本（起点）
  ↓ 追加
25本到達（2026-07-15）
  ↓ 追加
50本到達（2026-07-21）← 現在ここ
  ↓ 次はFounder Review
  Search Console・利用状況・note相性・収益化可能性を確認し、
  分類（育てる/守る/置いておく）を実データで見直す。
  新規公開は一旦停止し、反応のあるものを中心にブラッシュアップする。
```

1本を最初から完成させすぎない。**70点で公開し、反応が出たツールにだけ
追加投資する。** 大規模改修より新規公開を優先する。収益化実験は
同時多発させず、1つずつ検証する。

## 2026年8月運用方針（2026-08-04確定）

VIGOR TOOLS 50本・VIGOR MATSURI 78件到達を受け、8月は新規拡大より
「既存の仕組みを安定運用しながら、人が反応する入口と最初の収益導線を
見つける」ことを主目的とする。

```
VIGOR TOOLS 50本 + VIGOR MATSURI 78件 到達（2026-08-04）
  ↓
8月：安定運用フェーズ
  既存Scheduled Tasksの安定運用／note週1本／アフィリエイト実験1本のみ
  ↓ 30日後
  継続・修正・判定保留・停止のいずれかを判断し、9月方針へ反映
```

### 方針

1. 既存Scheduled Tasks（matsuriデータ更新・note下書き作成）を2〜4週間
   回し、品質と競合を確認する
2. noteを毎週1本、人間レビュー後に公開する（自動公開はしない）
3. 各note記事からVIGORの関連ページへ導線を1つだけ置く
4. 最初の収益実験はガジェット記事（Ray-Ban Meta関連）への
   アフィリエイト1本に絞り、段階的に進める
   （詳細は下記「アフィリエイト実験の段階化」）
5. 新しい大型プロジェクトは増やさない

### VIGOR MATSURIの方針（新規祭りの追加公開を止めるわけではない）

「新規拡大より安定運用」は、新規祭りの追加公開そのものを止める
という意味ではない。既存の運用ペースで続けるものと、今月着手しない
ものを区別する。

**継続すること：**
- 既存スキーマによる祭りデータの追加
- 既存祭りデータの更新
- データレビュー
- 既存Scheduled Tasksの安定運用

**今月やらないこと（MATSURI）：**
- 新しい祭りカテゴリの新設
- 大型機能の追加
- 新しい別サービスの立ち上げ
- VIGOR TOOLSの再量産

### VIGOR TOOLSとLAB全体で今月やらないこと

- アフィリエイト以外の収益チャネル（広告・スポンサー等）の並行検討
- 新しいScheduled Taskの追加
- noteの自動公開化（人間レビューは外さない）
- サイト全体のUI刷新・ブランド変更

### アフィリエイト実験の段階化

**第1週：**
- 対象候補ページ（Ray-Ban Metaチェック／ガジェット購入チェック）の
  GA4・Search Console基準値を保存する
- noteから対象ページへの導線を1本作る
- 利用するアフィリエイトサービスの規約と表示方法を確認する
- 外部リンククリックがGA4で計測できるか確認する

**第2週以降：**
- 対象ページへの流入が確認できた場合のみ、1ページ・1商品ラインで
  実験する
- リンクは購入判断結果の後にだけ置く（判定ロジックは変更しない）
- 判定ロジックや文章を報酬目的で変更しない
- 「アフィリエイトリンクを含みます」と淡々と明記する

### Distribution実験（note→moving-cost、2026-08-18開始）

「作ったものを知らない人にどう届けるか」を検証する最初の実験。新しい
集客施策を増やすのではなく、既存の週1note枠を初めてDistribution実験
として使う位置付け（詳細な設計判断の経緯は2026-08-18のFounderレビュー
記録を参照）。

```
公開日：2026-08-18
記事：引越し費用シミュレーター（tools/life/moving-cost/）紹介note
下書き：_notes-draft/2026-08-18-moving-cost.md
導線URL：https://vigorlab.net/tools/life/moving-cost/
  ?utm_source=note&utm_medium=article&utm_campaign=moving-cost
観測期間：2026-08-18〜2026-08-25（7日間）

主KPI（GA4でutm_campaign=moving-costのセッション数のみを見る）
  0件　　　→ 失敗。note PV・導線クリック状況を見て次の仮説を立てる
  1件以上　→ 最初の送客成功
  3件以上　→ 良いシグナル

参考指標（判定には使わない、観測のみ）
  note記事のPV・スキ数
  サイト全体の週次アクティブユーザー数
  （MATSURI季節変動・Organic Search影響を受けるため主KPIから除外）
```

TOOLSとMATSURIは集客特性が異なる（MATSURIは検索意図が明確な季節商材、
TOOLSは文脈説明を要するnote向き）ため、今回はTOOLS側のみを対象とし、
MATSURIの集客実験は変数を混ぜないため別サイクルで扱う。

### Distribution実験2（note→car-camping-check、キュー済み）

moving-cost実験（上記）完走・結果レビュー後に開始する。新規Toolは
作らない。既存の「ハイエースで車中泊を始めて2年。足してきた装備。」
記事へ、既存の`tools/travel/car-camping-check/`への導線を1つだけ
追加する（新規の続編記事は書かない。新記事自体の集客力という変数を
増やさないため）。

検証したいのは「すでに読まれている記事に、内容と直接一致する既存
Toolへの導線を置いたら、読者をVIGORへ送客できるか」の1点。

```
開始条件：moving-cost実験（2026-08-25終了予定）の結果レビュー後
記事：ハイエースで車中泊を始めて2年。足してきた装備。（既存記事へ導線追加のみ）
導線URL：https://vigorlab.net/tools/travel/car-camping-check/
  ?utm_source=note&utm_medium=article&utm_campaign=car-camping-check
観測期間：開始日から7日間

主KPI（GA4でutm_campaign=car-camping-checkのセッション数のみを見る）
  0件　　　→ 送客確認できず
  1件以上　→ 送客成功
  3件以上　→ 良いシグナル

参考指標（判定には使わない、観測のみ）
  記事側のビュー・スキ数の変化
```

moving-cost・car-camping-checkの2実験が完走した時点で、「note→Tool
というDistribution経路そのものが成立するか」「反応のあるテーマとの
一致が送客に影響するか」を確認する。「需要観測→Tool化」を正式な
新規ツール作成ルールにするかどうかは、この2実験の結果を見てから
判断する（現時点では未採用）。

### 8月に追う指標

- Search Console：対象ページの表示回数、クリック数、平均掲載順位
- GA4：対象ページの閲覧数、ユーザー数、平均エンゲージメント時間
- GA4または利用サービス：アフィリエイトリンククリック数
- note：記事ビュー、スキ、VIGORページへの遷移
- 全体：VIGOR TOOLSとMATSURIの月次推移

### 毎週確認する項目

- Scheduled Tasks（matsuriデータ更新・note下書き作成）が
  エラーなく動いているか
- 生成されたnote下書きが滞留せず、人間レビュー→公開まで
  進んでいるか
- 対象ページのPVに急変がないか（詳細分析は月末でよい）
- 新しい大型施策を自分から提案していないか
  （スコープクリープの自己点検）

### 30日後の判断条件

**継続：**
- アフィリエイトクリックが1件以上発生した
- または対象ページへの検索・note流入が増加傾向にある

**修正：**
- 対象ページに一定の閲覧があるが、リンクがクリックされない
- 導線位置、説明、記事との文脈を見直す

**判定保留：**
- 対象ページの閲覧母数が少なく、導線の良し悪しを評価できない
- 収益化を広げず、入口づくりを継続する

**停止：**
- 十分な閲覧があり、導線を修正しても反応がない
- または記事の信頼性を損なうと判断した

## 新規ツールの公開基準

新しいツールは、次をすべて満たすものに限定する。

```
- 一つの壁に絞られている
- 数分で使い終わる
- 入力や操作が複雑すぎない
- 結果を断定しすぎない
- 使った後の一歩が分かる
- 既存ツールと役割が重複しすぎない
- 「もやもやを使えるカタチにする」（VIGOR LAB Charter Mission）と
  接続している
```

## 公開済み25本の分類（育てる・守る・置いておく）

**この分類は仮説であり、確定事項ではない。** 25本到達時のFounder Reviewで、
実データを見ながら見直す。「置いておく」も反応があれば「育てる」に戻し、
「育てる」も反応がなければ優先順位を下げる。「守る」も、思想を壊さない
範囲での改善・関連ツールへの中立導線までは可能（現状維持を絶対条件にしない）。

**2026-07-15追加の12本は、意図的に未分類のまま。** 25本到達を待って
Founder Reviewで初めて分類する（先に分類してから作るのではなく、
公開してから反応で分類する、という今回の方針そのもの）。

```
育てる（6本）
  独立準備チェック・退職チェック・サブスク年間総額計算・
  AI作業仕分けシート・固定費チェック・ガジェット購入チェック

守る（4本）
  生活防衛資金シミュレーター（ブランドの中心順位1位。収益化はしないが、
    独立準備チェック等への中立導線は育てる）
  続ける理由vs辞めたい理由比較シート（Philosophy純度のアンカー）
  15分Do変換ツール・DCApアジャイルシート（思想の入口）

置いておく（3本）
  Ray-Ban Meta購入前チェック（ガジェット購入チェックに一般化済み）
  旅行準備チェック・引越しチェック（中核ナラティブの外側。
    公開継続・追加投資はしない。反応があれば再評価する）
```

### 反応ベースの重点化ルール（2026-08-14追加・TOOLS）

「育てる・守る・置いておく」の考え方自体は維持する。

- 守る：反応に関係なく維持・改善する（今回の反応ベース選別の対象外）
- 育てる：反応を見ながら重点投資する
- 置いておく：現状維持

その上で、「育てる」対象の選定・優先順位付けにSearch Console/GA4の反応を
用いる。50本を均等に改善するのではなく、反応が出たツールを優先的に
レビュー・改善する運用とする。

反応は2段階で扱う。

- 初表示（Search Console表示回数が初めて0超になった）：
  ウォッチリストに追加し、複数週継続するか観察する。この時点では
  改善投資はしない
- 初クリック（実際のクリックが初めて発生した）：
  重点レビュー対象へ昇格し、改善候補として扱う

Roadmap自体の変更ではなく、当面のPM運用方針として扱う。

---

## 運用ルール

- 固定計画ではない。**追加・削除・並び替えは自由**。
- 各アイテムは「仮説」。公開後の実際の反応（検索流入・リピート・回遊）で
  優先度が変わることを前提とする。
- 並び替え・追加・却下をしたら、下部の**更新履歴**に一言残す。
- ここに載っていないツール案が出てきたら、まずこの一覧に追加してから検討する。
  いきなり実装に進まない。

---

## Backlogの構成：Tools と Platform

Backlogは2種類の異なるものを扱う。

```
Tools
  ユーザーの個別の迷いを整理する、単体のツール。
  例：生活防衛資金シミュレーター、サブスク年間総額計算

Platform
  ツール横断で機能する、サイト全体の基盤機能。
  例：判断履歴、マイVIGOR、共通検索
```

Toolsは「1本ずつ公開して反応を見る」対象。
Platformは「サイトの前提を変える」対象なので、より慎重に、
小さい単位で検証しながら進める。

---

# Tools

## 現在の最有力候補 TOP3（ブランドらしさ最優先で選定・2026-07-14）

**3本すべて公開済み（2026-07-15完了）。** 次の優先順位はSearch Consoleの
検索パフォーマンスを見た上で、ChatGPT（Product Lead）と再検討する。

「検索流入・収益化・実装コスト」ではなく、
**「この1本を公開したら、VIGOR TOOLSらしさが一番伝わるか」**で選定。

```
1位　生活防衛資金シミュレーター「辞めても何ヶ月生活できる？」
     VISION.md Visionに名指しで書いた検索ワード「退職 何ヶ月」を実現する1本。
     このサイトで最も感情的重みの大きい数字を、判定せず本人に返す
     （Tool Principle 2を最高難度の場面で貫く）。
     Platform「判断履歴」の最初の検証先も兼ねる（下記参照）。

2位　サブスク年間総額計算
     同じくVISION.md Visionに名指しの「サブスク 年間 いくら」を実現。
     実装が最も軽く、最速で憲法どおりに動けることを証明できる。

3位　続ける理由 vs 辞めたい理由 比較シート
     判定・スコア・診断結果を一切出さない、唯一の純粋な整理ツール。
     「自分で決められる状態を作る」を最も純粋な形で体現する。
```

## 次の12本（25本到達に向けて・2026-07-15追加、同日全12本公開済み）

**12本すべて公開済み（2026-07-15）。VIGOR TOOLSは合計25本になった。**
次はFounder Reviewまで新規公開を止め、Search Console・利用状況を見る。

| 優先 | ツール名 | タイプ | 壁 | 接続先 | 将来収益化 | 状態 |
|---|---|---|---|---|---|---|
| 4 | 退職の伝え方テンプレート | テンプレート | 上司にどう切り出せばいいか分からない | 退職チェック／続ける理由vs辞めたい理由 | 中（退職代行は要注意） | 公開済み |
| 5 | やらないことリスト | テンプレート | 何かを増やす前に、何をやめるか整理できていない | 15分Do／DCAp | 低 | 公開済み |
| 6 | PC購入チェック | チェックリスト | このPC、失敗しないか分からない | ガジェット購入チェック | 高（Amazon等） | 公開済み |
| 7 | 転職チェック | チェックリスト | 転職していいか、判断材料が整理できていない | 続ける理由vs辞めたい理由／退職チェック | 中（転職エージェント） | 公開済み |
| 8 | 保険の入りすぎチェック | チェックリスト | 保険料が適正か分からない | 固定費チェック | 高（保険比較・FP） | 公開済み |
| 9 | AIに聞くべきか、人に聞くべきか 判断シート | 比較シート | AIに相談すべきか、専門家に相談すべきか迷う | AI作業仕分けシート | 低 | 公開済み |
| 10 | 独立後の目標日給計算 | 計算 | 独立後、いくら稼げばいいか分からない | 独立準備チェック／生活防衛資金シミュレーター | 中（会計ソフト） | 公開済み |
| 11 | 引越し費用シミュレーター | 計算 | 引越しにいくらかかるか把握できていない | 引越しチェック | 中 | 公開済み |
| 12 | 旅行予算シミュレーター | 計算 | この旅行、いくらまで出せるか分からない | 旅行準備チェック | 中（旅行・クレカ） | 公開済み |
| 13 | AI利用料、元取れてる？計算 | 計算 | AIサブスク、払い続ける価値があるか分からない | AI作業仕分けシート | 低（アフィリ先が少ない） | 公開済み |
| 14 | 「元取れてる？」損益分岐点チェック（汎用版） | 計算 | このサブスク・会員、継続する価値があるか分からない | サブスク年間総額計算／固定費チェック | 中 | 公開済み（判定なし版に変更） |
| 15 | 副業しどき診断 | 診断 | 副業を始めていい時期か分からない | 独立準備チェック／退職チェック | 高（副業マッチング） | 公開済み |

タイプ内訳：テンプレート2／チェックリスト3／比較シート1／計算4／診断1。

**設計上の変更点（14番）：** 損益分岐点チェックはBacklogでは「計算＋判定」
としていたが、実装時に判定を持たせなかった。継続課金の妥当性を判断する
汎用的な閾値を安全に定義できず（ジムと動画配信では"良い1回あたりコスト"
が全く違う）、断定を避けるTool Principle 2を優先し、他の計算ツールと
同じ「数字だけを返す」設計にした。

---

# Platform

サイト全体の基盤機能。個別ツールと違い、一度入れると
全ツールの設計に影響するため、最小単位で検証しながら進める。

| 状態 | 機能名 | 説明 |
|---|---|---|
| 検証中 | 判断履歴 | ツールを使った結果を、ユーザーが明示的に残せる仕組み。トレンドやグラフは持たず、ツールごとの「最新の結果」だけを記録する。生活防衛資金シミュレーター・サブスク年間総額計算・続ける理由vs辞めたい理由比較シートの3本で稼働中。 |
| 検証中 | 共通データ（Profile） | 複数ツールで再利用する「入力値」を保存する仕組み。判断履歴（結果のログ）とは別物。サブスク年間総額計算→固定費チェック／生活防衛資金シミュレーターの1本目の連携で稼働中。 |
| 保留 | マイVIGOR | 判断履歴・共通データが自然に育った先に生まれる、状態が見えるホーム画面。両方の価値が検証できるまで着手しない。 |
| 保留 | 共通検索 | ツール数が増えた際の横断検索。目安としてツールが20本を超えたあたりで検討。 |
| 実装済み | 内部トラフィック除外 | Founder自身のアクセスをGA4データから除外する仕組み。IPアドレス方式は複数環境（自宅/会社/旅行先/モバイル）に弱いため、localStorageフラグによる送信抑制方式へ切替済み（2026-08-14）。 |

### 判断履歴 v0.1（検証スコープ）

```
- 対象ツール：生活防衛資金シミュレーター、サブスク年間総額計算、
  続ける理由vs辞めたい理由比較シート（3本で検証中）
- 保存内容：ツールごとの最新の結果1件（時系列やトレンドは持たない）
- 保存先：localStorage（サーバー送信なし、ログイン不要）
- 見せ方：ツール側に「この結果は判断履歴に保存されました。」の一言
  ＋「判断履歴を見る」リンクのみ。専用ホーム画面はまだ作らない。
- 検証すること：履歴が残ること自体が、ユーザーにとって嬉しいか
```

3本での検証が揃った。反応が良ければ、独立準備チェックなど4本目を追加し、
その先で初めて「マイVIGOR」の着手を検討する。

### 共通データ（Profile）v0.1（検証スコープ・2026-07-15新設）

```
- 目的：一度入力した数値を、何度も入力させない
- 対象フィールド：monthlySubscriptionTotal（サブスク月額合計）のみ
- 保存先：localStorage（vigor_profile。判断履歴とは別のキー）
- 保存方式：ユーザーが明示的にボタンを押した時だけ保存する（自動保存はしない）
- 見せ方の原則：数値を「自動入力」せず「参考値として表示」する。
  固定費チェックはサブスク合計を要約表示するが、生活防衛資金シミュレーターの
  「毎月の生活費」フィールドには自動入力しない
  （サブスクは生活費の一部でしかなく、自動入力すると数値が過小になり
  誤解を招くため。Tool Principle 8「前提と責任範囲を明記する」に基づく判断）
- 将来の展開候補：固定費チェック自体に数値入力を持たせるかどうかは
  別途検討（現状はチェックボックスのみで、合計金額を持たない）
```

反応が良ければ、独立準備チェック・退職チェックなど、
固定費データを使う他ツールへの接続を追加する。

**設計上の制約（Guard Rails）は `shared-profile.js` の冒頭コメントを参照。**
フィールドを追加・拡張する前に必ず読む。

### 内部トラフィック除外（2026-08-14実装）

```
- 対象：assets/analytics.js（root共通、TOOLS・MATSURI・LAB全体に自動適用）
- 仕組み：?internal=1 でlocalStorage（vigor_internal_traffic）にフラグを
  保存したデバイス・ブラウザは、以後GA4への送信自体を行わない
  （GAスクリプトの読み込みごと止める。IPアドレスには一切依存しない）
- 解除：?internal=clear（0は後方互換エイリアス）
- 運用ルール・デバッグ方法はassets/analytics.js冒頭のコメントを参照
  （常にコードと同期させるため、ランブックはコード側のみに置く）
- 旧IPベースのGA4管理画面Internal Traffic Ruleは本機構への切替に伴い
  廃止対象（Founder側でGA4管理画面の操作が必要）
```

---

## 技術整理タスク（未着手）

```
2026-07-29
  tools/thinking/dcap/CLAUDE.md に、存在しない外部ファイルへの
  「MUST READ」参照が残っている
  （@../../aidlc-workflows/aidlc-rules/aws-aidlc-rules/core-workflow.md）。
  同ディレクトリの aidlc-docs/（requirements.md・workflow-plan.md・
  aidlc-state.md・audit.md・.claude/settings.local.json）とあわせて、
  DCApアジャイルシートv2開発時に使ったAWS AI-DLCワークフロー実験の
  名残と見られる。参照先が存在しないため実害はないが、紛らわしいため
  整理対象。tools/CLAUDE.md作成時の調査（2026-07-29）で発見。
  対応（削除 or 現状維持）はtools/配下の実務ルール整備とは別タスクとし、
  統括（Founder）判断待ち。

2026-07-29
  Platform「判断履歴」のBacklog記載とコード実態が乖離している。
  本書のPlatformセクション（判断履歴 v0.1）は「対象ツール3本（生活防衛
  資金シミュレーター・サブスク年間総額計算・続ける理由vs辞めたい理由
  比較シート）で検証中」と記載しているが、Founder Review準備の棚卸し
  （2026-07-29）でコードを確認したところ、shared-history.jsは実際には
  30本のツールで導入済みだった。記載が更新されないまま実装が先行した
  状態。card-status.js（一覧カードの進捗バッジ）は3本のみに限定されて
  おり、判断履歴30本の実導入規模に追いついていない、という表示側の
  ギャップも別途ある。
  対応（Backlog記載の更新／card-status.jsのバッジ対象拡大）はPhase 2
  （軽量改善）の候補として扱い、今回は記録のみで着手しない。

2026-08-21
  MATSURI祭りページの動画検索（Search Console）表示回数が週次で
  56→0のように大きくブレる現象を調査した。動画自体（YouTube埋め込み、
  atmosphereMedia）は126祭り中76件に存在し欠落はない。原因は、動画を
  検索エンジンへ伝える正式な仕組み（VideoObject構造化データ・動画
  サイトマップ）が一切なく、動画iframeもJavaScriptによる動的埋め込み
  （生HTMLには存在しない）のみのため。Googleがクロール時にJS実行で
  発見する間接的な手段だけに依存しており、母数も小さい（週56件程度）
  ため不安定。
  「壊れた」のではなく元々土台が弱い状態。対応（VideoObject構造化
  データの追加）は新規実装作業になるため、8月の安定運用方針に沿って
  今回は記録のみとし、着手しない。
```

---

## 却下・保留

（却下ではなく、保留中の改善候補）

```
2026-07-24
  会社員のうちにやることリスト：セクション内の要素順を
  「カテゴリ説明→詳しく見る→チェック項目」から
  「カテゴリ説明→チェック項目→詳しく見る」へ変更する案。
  Founderコメント：チェックリストとしてのテンポを優先するなら、
  詳細を読まなくてもチェックを進められ、必要な人だけ後から
  補足を開ける順番の方がよいのでは、という提案。
  今回のUI仕上げ（詳しく見るのデザイン修正）はv2として承認済み。
  順序変更は見送り、改善候補として保留。着手する場合はこのツール
  1本で先に検証してから、他のチェックリスト型ツールへの展開を判断する。
```

```
2026-08-04
  「独立準備計算ツールシリーズ」10本（一括追加案、Founderからは
  「本当は30本くらい増やしたい」との言及あり）を却下。
  却下理由：
  - 10本中9本が既存ツールと重複（詳細は下記）。新規性があるのは
    「複数収入源の依存度計算」1本のみ。
  - 2026年8月運用方針（本書上部、2026-08-04確定）の
    「新しい大型プロジェクトは増やさない」「VIGOR TOOLSの再量産はしない」
    と、tools側Phase1（計測待ち・新規ツール追加保留、2026-07-29確定）の
    双方に反する。
  - 50本→60〜80本という量的拡大は、Founder Review準備が目指した
    「選抜・改善」の逆行にあたる。
  重複マッピング：
    独立後の最低売上ライン／会社員収入を置き換える売上計算
      → work/target-daily-rate・money/independence-fixed-costs・
        money/break-even-check と重複
    独立まで何ヶ月／固定費削減による独立時期短縮
      → money/savings-gap・money/savings-pace・money/cost-cut-impact
        の組み合わせで代替可能
    退職時期比較シミュレーター
      → work/resignation-check・stay-or-leave・job-change-check・
        before-quitting-checklist・resignation-script と重複
        （退職系だけで既に5本、新たな重複クラスタとして認識）
    独立後の生活可能月数計算 → money/survival-fund とほぼ同一計算
    副収入の固定費カバー率／月1万円の副収入の販売件数
      → money/fixed-cost-check・money/side-income-target の合成
    副業の実質時給 → work/side-time-income とほぼ同一目的
  新規候補として保留（実装はしない）：
    「複数収入源の依存度計算」のみ。判定的な数値を出す設計は
    Tool Principle 2（整理はする、決断はしない）に抵触しやすいため、
    実装するとしても「数値提示のみ・判定なし」の設計が前提。
  着手時期：Phase1（計測導入完了）→Phase3（データに基づく再評価）
  以降。それまでBacklog記録のみで実装指示は出さない。
```

```
2026-08-04（続報：方針転換）
  新規10本の却下は維持しつつ、Founderから「計算結果から次の確認へ
  進める体験を強化したい」という制作意欲を活かす代案が出た。
  新規追加ではなく、既存の計算系10本を段階的にテンプレート化する方針
  に転換。

  型：「計算結果 → 確認チェック → 関連ツール」
  追加要素：(1)結果の一文説明 (2)確認項目3〜5件 (3)関連ツール最大2本
  (4)計算式・前提の<details>開閉表示 (5)単位表記統一 (6)既存Profile値の
  再利用。条件変更時の比較機能は対象外（Phase3以降、実データを見てから
  設計する）。

  この改修は計測データに依存しないUX一貫性の底上げであり、Phase1
  （計測待ち）中でも実施可能と判断。tools側Phase2「役割重複クラスタの
  導線整理」の範囲内として扱う。8月運用方針の禁止事項（新しい大型
  プロジェクト・サイト全体のUI刷新）には該当しない：既存の
  .checklist／.related-tools／ネイティブ<details>を再利用する軽量改修
  であるため。

  段階：
    Phase 1（パイロット）：survival-fund／fixed-cost-check／
      subscriptionsの3本のみ実装。横展開はしない。
    Phase 2（横展開）：型が安定した場合のみ、independence-fixed-costs／
      savings-gap／savings-pace／target-daily-rate／side-income-target／
      side-time-incomeの6本へ展開。
    Phase 3（特別対応）：break-even-checkは判定ロジックを持たせない
      設計に変更した経緯があるため、確認項目の文言が実質的な判定に
      転ばないか単独レビューしてから最後に適用。

  実装前調査で判明した現状（パイロット3本）：
    survival-fund：related-tools 4件 → 2件へ絞り込みが必要
    fixed-cost-check：related-tools 4件 → 2件へ絞り込みが必要。
      また.checklistを本来の入力UIとして既に使用中のため、新設する
      「確認項目」ブロックと視覚的に混同しないデザイン差別化が必要
    subscriptions：related-tools 2件で既に適合、内容の妥当性のみ確認

  パイロット3本の実装をCodex CLIへ指示済み（2026-08-04）。
  横展開（Phase2以降）は品質確認後にあらためて判断する。
```

```
2026-08-04（パイロット完了・レビュー済み）
  コミット55702bf。survival-fund／fixed-cost-check／subscriptionsの
  3本に型を実装。Claudeが差分を実地検証（報告のみに依拠せず）。

  確認できた内容：
  - related-tools を4→2件へ絞り込み（survival-fund・fixed-cost-check）、
    選定理由も明記
  - fixed-cost-checkの懸念点（新設「確認すること」チェックリストが
    既存23項目集計と混同するリスク）は`#cost-checklist`でDOM/JS両方を
    スコープ分離し解消済みとコードで確認（23項目のみ集計対象）
  - survival-fundの旧4段階判定文言を完全撤去し中立的な説明へ変更。
    指示（判定や断定を強めない）を踏み込んで実行
  - 削除箇所に対応する死にCSSも残さず除去、スコープ外ファイルへの
    混入なし（変更9ファイルのみ）

  軽微な指摘（ブロッカーではない）：
  - shared.cssの新規スタイルに`#fixed-cost-details`というID依存
    セレクタが1箇所混在。Phase2（残り6本への横展開）着手時に
    `.result-section details`のクラス指定へ統一し解消する。

  判定：承認。追加修正なしでパイロット完了。横展開（Phase2）は
  Founderの判断を待って着手する。
```

```
2026-08-04（Phase2実施・レビューでバグ発見）
  Founder承認を受けPhase2着手。コミット2528adaで残り6本
  （independence-fixed-costs／savings-gap／savings-pace／
  target-daily-rate／side-income-target／side-time-income）に型を
  展開。Codexが実装・コミットまで対応し、Claudeが差分を実地検証した。

  良かった点：6本ともトーン一貫（断定なし）、計算式説明は実装と
  整合、related-toolsは元々2件で変更不要と正しく判断、変更範囲も
  指示通り。

  発見したバグ：このコミットでshared.cssの`#fixed-cost-details`
  （前回指摘したID依存セレクタ）を`.result-section details`へ
  一般化したが、fixed-cost-check（チェックリスト型ツールのため
  `.result-section`でラップされていない）の`<details>`がどの
  CSSルールにもマッチしなくなり、枠線・余白のスタイルが消失。
  実機確認済み。
  修正方針：`<details>`要素自体に共通クラス（例：calc-detail）を
  付与し、DOM構造（`.result-section`配下か否か）に依存しない
  セレクタへ統一する。Phase3のbreak-even-check適用時も同じ問題が
  起きうるため、この方式に統一しておく。
  対応：Codexへ修正指示済み。
```

```
2026-08-05（回帰バグ修正完了・レビュー済み）
  コミットbc53c73。shared.cssに`.calc-detail`クラスを追加し、
  計算系9本（fixed-cost-check／survival-fund／subscriptions／
  independence-fixed-costs／savings-gap／savings-pace／
  side-income-target／side-time-income／target-daily-rate）の
  `<details>`へ適用。fixed-cost-checkは既存`id="fixed-cost-details"`
  を維持しつつクラス追加。break-even-checkは`<details>`が存在せず
  対象外（Phase3で個別対応の前提と一致、未変更を確認）。

  Claudeが差分を実地検証：shared.cssのセレクタ追加、9本全ての
  クラス付与、break-even-check不変、変更ファイル範囲（10ファイル）
  いずれも指示通りと確認。判定：承認、追加修正なし。

  これでPhase2（既存10本のテンプレート化）は
  fixed-cost-check／survival-fund／subscriptions／
  independence-fixed-costs／savings-gap／savings-pace／
  side-income-target／side-time-income／target-daily-rateの9本が
  完了。残るbreak-even-check（Phase3・単独レビュー対象）のみ未着手。
```

```
2026-08-05（Phase3完了・計算系10本すべて完了）
  break-even-checkを単独レビューしたうえで型を適用。判定ロジックを
  持たせない過去の設計判断（2026-07-15、汎用閾値を安全に定義できない
  ため）は維持を確認し、テンプレート適用前にClaudeがレビュー・
  修正方針を提示、Founder承認後にCodexが実装（コミット6a7cc5c）。

  レビュー観点と結果：
  - 判定ロジックは追加せず維持（既存方針を踏襲）
  - 既存の断定寄りの一文「続ける価値があるか考えてみてください」を
    削除し、result-note（事実説明）／calc-detail（計算式・前提）／
    確認チェック4件に分割。「高い／安い／妥当」等の評価語は
    使用していないことを差分で確認
  - related-tools（既存2件）・見出し・page-desc・計算ロジックは
    いずれも未変更を確認（script.jsの差分ゼロ）

  これで計算系10本（survival-fund／fixed-cost-check／subscriptions／
  independence-fixed-costs／savings-gap／savings-pace／
  side-income-target／side-time-income／target-daily-rate／
  break-even-check）すべてが「計算結果→確認チェック→関連ツール」の
  型へのアップグレードを完了。新規ツール追加ゼロで実施。

  次のアクション：横展開の完了報告として区切りとし、追加の改善は
  Founder判断待ち。計測基盤（Phase1）の状況を踏まえ、次の優先順位は
  改めて検討する。
```

---

## 更新履歴

```
2026-07-14（初版）
  VISION.md v1に基づき10本をブレスト。
  「ブランドらしさ最優先」の視点でTOP3を再選定
  （生活防衛資金シミュレーター／サブスク年間総額計算／
    続ける理由vs辞めたい理由比較シート）。

2026-07-14（追記）
  BacklogをTools / Platformの2部構成に再編。
  「判断履歴」をPlatformの第一歩として追加。
  「マイVIGOR」「共通検索」は判断履歴の価値検証後まで保留。
  生活防衛資金シミュレーターを、判断履歴の最初の検証先として指定。

2026-07-15
  サブスク年間総額計算を公開。判断履歴の2本目の検証先を兼ねる。
  新たに「共通データ（Profile）」をPlatformに追加。
  サブスク年間総額計算→固定費チェック／生活防衛資金シミュレーターの
  データ連携をv0.1として実装（自動入力ではなく参考値表示の設計）。
  Profile Platform Guard Rails（8項目）をshared-profile.jsに明記。

2026-07-15（続報）
  続ける理由vs辞めたい理由比較シートを公開。TOP3が3本すべて公開済みに。
  判定・スコア・件数比較を一切出さない設計を徹底し、判断履歴には
  「行為の記録」のみを残す（例：「〇〇について書き出した」）。
  次の優先順位はSearch Console検索パフォーマンスを見た上で再検討する。

2026-07-15（方針転換）
  「深く育てる」から「広く作って反応で深める」へ方針転換。
  13本→25本→Founder Review→50本、というフェーズ計画を追加。
  公開済み13本を育てる/守る/置いておくに暫定分類（確定ではなく仮説）。
  次の12本の候補（テンプレート2・チェックリスト3・比較シート1・
  計算4・計算＋判定1・診断1）を追加し、実装コスト順に並べた。
  新規ツールの公開基準（7項目）を明文化。1本を70点で公開する方針を確認。

2026-07-15（12本一括公開）
  次の12本を全て公開。VIGOR TOOLSは合計25本に到達。
  損益分岐点チェックは設計時点で「計算＋判定」としていたが、汎用的な
  閾値を安全に定義できないため判定を外し、他の計算ツールと同じ
  「数字だけを返す」設計に変更した。新規12本は意図的に育てる/守る/
  置いておくへ分類せず、Founder Reviewまで保留する。
  次のアクションはFounder Review（Search Console・利用状況・
  note相性・収益化可能性の確認）。新規公開は一旦停止する。

2026-07-16（発見しやすさの改善・新規ツールなし）
  25本到達を受け、新規ツール追加を止め「見つけやすさ」の改善に着手。
  トップページの「困りごとから探す」を4カード→6カードに拡張
  （まず何か動きたい／辞める・独立を考えている／お金を整理したい／
  買うか迷っている／AIを仕事に使いたい／考えを整理したい）。
  25本中18本がここから到達可能（同一ツールが複数カードに登場するのは
  意図的な設計）。残る7本（旅行・暮らし系中心）はカテゴリ一覧から到達する。
  「まずはここから」を15分Do／続ける理由vs辞めたい理由比較シート／
  サブスク年間総額計算の3本に再選定
  （数分で終わる・入力が軽い・次の一歩が明確・Philosophyが伝わる、の基準）。
  生活防衛資金シミュレーターは初回訪問者には重すぎる入力（貯蓄額）を
  求めるため、あえて「まず使う3本」から外し、困りごとカード経由の
  導線として扱うことにした。
  「次はこちら」のリンクが1本しかなかった5ツール
  （旅行予算シミュレーター・引越し費用シミュレーター・PC購入チェック・
  AIに聞くべきか人に聞くべきか判断シート・AI利用料計算）に1本ずつ追加し、
  全25本が2本以上を確保。
  カテゴリ件数表示の不整合（12本追加後に更新されていなかった）を修正。
  「最近追加」を直近12本のうちの3本に更新。
  既存の.worry-gridのCSSに、モバイル1カラム化が効かなくなっていた
  バグ（メディアクエリの定義順序の問題）を発見・修正。今回の変更で
  困りごとカードのリンク数が増えたことで顕在化したが、原因は
  以前からの既存バグ。
  新規UIコンポーネント・新規JSロジックは追加していない
  （既存のworry-card/related-itemパターンを流用）。

2026-07-21（25本一括公開・50本到達）
  Founderの直接指示により、25本を1開発サイクルで一括公開。
  当初の依頼は「27本→50本、23本追加」だったが、実際の公開数を
  確認したところ25本だったため、25本を追加して正確に50本に到達させた
  （差異はFounderへ都度報告）。
  内訳：AI活用3・仕事6・お金6・思考整理5・旅行3・買い物1・暮らし1。
  候補案のうち既存ツールと重複する3本は差し替え、代わりに
  「会社員のうちにやることリスト」「独立後の固定費見積もり」を
  新規に設計して数合わせした。
  全25本、スコア・判定は持たせず（Tool Principle 2）、既存の
  5アーキタイプ（チェックリスト／計算／比較／テンプレ＋コピー／
  単一テキストエリア）のみを流用。新規デザイン・新規JS機構は追加していない。
  sitemap.xml・7カテゴリページ・トップページ（カテゴリ件数・最近追加・
  困りごとカードへのリンク追加）を更新し、50本の表示を全箇所で整合。
  Playwright等の自動品質ゲート（E2E／axe-core／Lighthouse／
  Visual Regression／AI UXレビュー）は次のステップとして続けて実施する。

2026-07-29（Founder Review準備・3フェーズ方針を確定）
  50本到達を受けたFounder Review準備として、全50本の棚卸しと暫定分類
  （育てる21・守る4・様子見25）を実施。分類はすべて仮説であり、計測
  タグが1本も導入されていないことが確認できたため、Founderの判断で
  以下の3フェーズ方針を確定した。

  Phase 1（計測待ち）：統括担当のSearch Console／Analytics導入完了を
    待つ。見る指標はSearch Console側が表示回数・検索クエリ・CTR・
    掲載順位、Analytics側がPV・滞在・スクロール・ツール完了到達・再訪。
  Phase 2（軽量改善のみ）：計測を待つ間、新規ツール追加・大規模UI変更
    は保留し、以下のみ可とする。
      1. 役割重複クラスタ（元取れてる系4本／独立収入目標系3本／
         貯金計算2本／無色の比較シート3本）の導線整理
      2. shared-history.js／card-status.jsのドキュメント整合
      3. Backlog.mdの古い記述更新
  Phase 3（Founder Review本番）：計測データ取得後、育てる候補・守る
    候補・統合候補・廃止検討候補を実データで再評価する。

  今回のFounder Review準備自体は判断材料の提示に留め、Phase 2の各候補
  も含めて実装には着手していない。計測基盤導入完了・データ取得期間を
  経てからPhase 3へ進む。

2026-08-05（GA4内部トラフィック除外をマーカー方式で実装）
  MATSURI PMレビューで、内部・検証アクセスの混入によりGA4データが
  意思決定に使いにくい状態と判断されたことを受け、統括PM判断として
  IPベース除外ではなく `?internal=1` クエリパラメータによるマーカー
  方式を採用した。対象はVIGOR TOOLS・VIGOR MATSURI共通のため、
  両者が共通で読み込む `assets/analytics.js`（143ページ）の1ファイル
  のみを変更し、個別HTMLは変更していない。

  実装：`?internal=1` でlocalStorageキー`vigor_internal_traffic`に
  "1"を保存、`?internal=0`で削除。フラグが立っている場合のみ
  `gtag("config", ID, { traffic_type: "internal" })` を呼ぶ。
  localStorage不可環境はtry/catchで通常計測にフォールバックする。
  実装はCodexに依頼し、統括PMがレビュー後にコミット・pushした。

  本番（vigorlab.net）でブラウザ実地検証を実施し、実際のGA4送信
  リクエストに `tt=internal`（`traffic_type`の短縮パラメータ）が
  含まれることを確認した。あわせて、`?internal=0`によるlocalStorage
  解除自体は即座に反映される一方、GA4のgtag.jsが同一セッション内では
  一度設定した`traffic_type`を保持し続ける挙動を確認した。このため
  `?internal=0`は「同一セッション中の即時解除」ではなく「次回セッション
  （30分の非アクティブ後、または新しいブラウザコンテキスト）からの
  解除」として機能する。個人開発の検証用途では実用上問題ないと判断し、
  この制限を仕様として記録するに留め、追加の作り込みはしない
  （過剰な仕組みを避ける方針に合わせた判断）。

  GA4側の設定手順（Founder作業）：
    1. GA4管理画面 → データ設定 → データフィルタ → 新規作成
    2. フィルタタイプ：内部トラフィック
    3. 運用状態を「テスト中」にし、数日〜1週間、除外イベント数が
       自分のアクセス実態と一致するか確認する
    4. 想定通りであれば「有効」に切り替える
  データフィルタは今後のデータにのみ適用され、過去データは遡って
  除外されない。フィルタ有効化日より前のGA4データは参考値として扱う。

2026-08-05（テスト期間開始・Founder作業の進捗）
  Founder作業でGA4データフィルタを作成済み（名称「Internal Traffic」、
  種類：内部トラフィック、動作：除外、条件：traffic_type が internal
  と完全一致、状態：テスト）。コード側（?internal=1／?internal=0と
  localStorage方式）が正しく反映されていることも確認済み。
  普段使う端末のうちChromeは`vigor_internal_traffic`のマーキングを
  確認済み。残りはSafari・スマホ等、他に使用する端末・ブラウザでの
  マーキングのみ。現在は約1週間のテスト期間中で、「有効」への切り替え
  はまだ行っていない。GA4設定・コードとも本エントリでの変更はなし。

2026-08-05（50本棚卸しレビュー・改善計画確定）
  50本を維持／改善／統合／削除候補に分類したPMレビューを実施
  （Artifact化：https://claude.ai/code/artifact/59479d04-594c-4089-b771-287e4ca69397）。
  維持43／改善候補5／統合検討2／削除候補1（番外：dcap-agile-sheet、
  既知・統括判断待ち）。

  訂正：レビュー初版でwork/before-quitting-checklistを
  「related-tools 0件」としたが誤り。抽出スクリプトが
  `<section class="related-tools" id="...">` のようにid属性が
  付いたタグを拾えていなかったための誤検知（他49本への影響はなし、
  この1本のみ）。実際は独立準備チェック・退職までに必要な貯金差額計算の
  2件が既にリンク済みで問題なし。改善対象から除外し、維持へ戻す。

  実装確認により、travel/checklistは当初想定より重い問題と判明：
  related-tools内のdiv構造が壊れており（`.related-list`が実際の2件で
  早期に閉じ、「準備中」プレースホルダー2件がラップ外に浮いた状態）、
  かつその「準備中」項目のうち1件（旅行予算チェック）は既に
  travel/budgetとして公開済みで重複、もう1件（雨の日旅行チェック）は
  実装されたことのない幻の参照。単なる関連ツールの絞り込みではなく
  バグ修正案件として扱う。

  改善計画（優先順） Founder承認済み：
    1. travel/checklist：壊れたdiv構造の修正、存在しない「準備中」
       2件の削除、リンク先をtravel/budget・travel/stay-compare
       （両者は既にchecklistへ相互リンク済み）へ差し替え
    2. money/insurance-check：チェック項目「保険料が月収に対して
       高すぎないか考えた」を中立表現へ調整、結果表示に「判定する
       ものではない」旨を追加。判定ロジック自体（count-based進捗）は
       不変
    3. money/cost-cut-impact：計算系10本と同型のテンプレート適用
       （related-toolsは既に2件で変更不要）
    4. ai/ai-cost-check：同上のテンプレート適用（related-toolsは
       既に2件で変更不要）
    5. 独立・退職ジャーニー15本：新規要素なし、既存related-toolsの
       リンク先入れ替え・文言微調整のみ（表示件数は現状維持）

  統合（ai-cost-check↔break-even-check、rayban-meta↔gadget-check）は
  今回のバッチで実行しない。既に相互リンク済みで実害小、URL統廃合は
  8月方針の「大型プロジェクトは増やさない」領域に踏み込みうるため、
  実行判断はPhase3（実データ取得後）に送る。

  1〜5は全て計測データに依存しないバグ修正・UX一貫性改善のため、
  Phase1（計測待ち）中でも実施可。Codexへは1から順に実装指示する。

2026-08-05（改善1/5完了：travel/checklist）
  コミット40b95d4。壊れたdiv構造を修正し、存在しない「準備中」
  プレースホルダー2件を削除。リンク先をthinking/15min-do・
  money/fixed-cost-checkからtravel/budget・travel/stay-compare
  （両者は既にchecklistへ相互リンク済み）へ差し替え。
  Claudeが差分を実地検証：div/section/mainのタグバランス正常、
  related-item 2件・related-item--coming 0件を確認。31項目・
  7セクションの本体、script.js、style.cssは未変更、変更範囲は
  指示通り1ファイルのみ。判定：承認、追加修正なし。
  次はmoney/insurance-check（判定色の強い文言調整）。
```

```
2026-08-05（改善2/5完了：money/insurance-check）
  コミットf942f8a。チェック項目「保険料が月収に対して高すぎないか
  考えた」を「保険料が今の収入・支出に対して無理のない範囲か考えた」
  へ調整。page-headerに「多い・少ない、妥当かどうかの判定はしません」
  のresult-noteを追加（新規CSSは既存.result-noteを再利用、追加なし）。
  Claudeが差分を実地検証：タグバランス正常、チェック項目9件維持、
  script.js差分ゼロ（判定ロジックは元々なく、変更もなし）。
  判定：承認、追加修正なし。
  次はmoney/cost-cut-impact（計算系10本と同型のテンプレート適用）。
```

```
2026-08-05（改善3/5完了：money/cost-cut-impact）
  コミット62e3699。計算系10本と同型（result-note／calc-detail／確認
  チェック4件）を適用。新規CSSなし、既存.calc-detail等を再利用。
  Claudeが差分を実地検証：タグバランス正常、script.js差分ゼロ、
  related-tools 2件維持。判定：承認、追加修正なし。
  次はai/ai-cost-check（同型のテンプレート適用）。
```

```
2026-08-05（改善4/5完了：ai/ai-cost-check）
  コミットf0b3194。既存.disclaimerを他10本と同型（result-note／
  calc-detail／確認チェック4件）へ置き換え。break-even-checkとの
  重複は今回統合せず、型の適用のみに留めた（統合実行判断はPhase3）。
  Claudeが差分を実地検証：タグバランス正常、script.js差分ゼロ、
  related-tools 2件維持。判定：承認、追加修正なし。
  次は独立・退職ジャーニー15本のrelated-tools導線見直し（改善5/5、
  新規要素なし・リンク先入れ替えのみ）。
```

```
2026-08-05（改善5/5：ジャーニー導線見直し・具体案をFounder承認）
  15本のリンクグラフを実地調査した結果、手を入れる根拠があるのは
  4ファイル・各1リンクの入れ替えのみと判断（他11本は既に妥当な
  組み合わせ）。
    work/resignation-check：15min-do → money/savings-gap
    money/independence-fixed-costs：work/independence →
      work/target-daily-rate
    work/target-daily-rate：money/survival-fund →
      money/independence-fixed-costs（上記と対になる相互リンク）
    work/side-business-check：ai/task-sort → money/side-income-target
  リンク件数は全ファイルで現状維持、新規要素の追加なし。Founder承認
  済み、Codexへ1本の指示としてまとめて実装依頼する。
```

```
2026-08-05（改善5/5完了・50本棚卸し改善計画すべて完了）
  コミット3df7c17。4ファイル各1リンクを承認済み内容通りに差し替え。
  Claudeが差分を実地検証：件数維持（resignation-check 4件・他3本
  各2件）、badge表記もリンク先の実際の種別と一致。「次の一歩として
  自然か」もレビューし、4件とも通過（退職チェック→貯金差額、固定費
  見積もり↔目標日給の相互リンク、副業診断→副収入目標、いずれも
  自然な流れ）。

  発見（記録のみ・今回のスコープ外）：axe指摘（4ファイル合計31件）は
  この4本が計算系10本ロールアウトの対象外だったため`<main>`
  ランドマークが未適用のまま残っていたことに起因すると判明。今回の
  リンク差し替えが原因でないことをコードで確認済み。他の未ロール
  アウトツールにも同様の`<main>`欠落が残っている可能性があり、
  横断的なaxe対応が必要なら別タスクとして扱う。

  これで2026-08-05の50本棚卸しレビューに基づく改善計画（travel/
  checklistのバグ修正／insurance-checkの文言調整／cost-cut-impact・
  ai-cost-checkのテンプレート適用／ジャーニー4本の導線見直し）が
  すべて完了。新規ツール追加ゼロ、既存資産の価値最大化のみで実施。
  次のアクションはFounder判断待ち。計測基盤（Phase1）の状況を踏まえ、
  統合検討2本（ai-cost-check↔break-even-check／rayban-meta↔
  gadget-check）を含むPhase3の再評価はデータ取得後に行う。
```

```
2026-08-19（car-camping-check：Distribution実験2送客前の項目整理・完了）
  コミット2c7d892。「ハイエースで車中泊を始めて2年。足してきた装備。」
  note記事からのDistribution実験2（本書上部「Distribution実験2」参照、
  moving-cost実験完走後に開始予定）に向け、送客先である
  tools/travel/car-camping-check/のチェック項目を送客前に軽く整理。

  PMレビュー（6観点）を経てFounder承認、Codex CLIへ実装指示。
  変更内容：
  - 削除（3件）：換気方法を確認した／車のドアロック・防犯対策を
    確認した／緊急連絡先・保険証を持った。行動確認・安全確認寄りの
    項目を外し、「持ち物」チェックへ焦点を絞った。換気は車中泊特有の
    安全上重要な観点だが、他に代替ツールがない中での意図的なスコープ
    外化としてFounderが明示的に判断
  - カテゴリ改名：「防犯・安全」→「生活用品」
  - 追加（枕／季節の暑さ寒さ対策／飲み物・水／タオル洗面用品／
    ゴミ袋／ティッシュウェットティッシュ）、表現調整（段差解消グッズ・
    充電ケーブルの具体化等）
  - 11項目→13項目・3カテゴリ（目標レンジ10〜15内）
  - meta description／og:description／twitter:descriptionの
    「防犯」表記もカテゴリ改名に合わせて「生活用品」へ更新

  script.js・style.cssは無変更（TOTALはcheckboxes.lengthから動的
  算出のため件数変更に伴うロジック修正は不要）。related-toolsは
  未変更。Claudeが差分を実地検証：タグバランス正常（div 7/7・
  section 4/4・ul 3/3・li 13/13）、チェックボックス13件、変更範囲は
  index.html 1ファイルのみ。判定：承認、追加修正なし。

  Codex CLI実行環境の制約メモ：sandbox実行時は`.git`への書き込み
  （git add/commit）が権限エラーで失敗するため、ファイル編集は
  Codexが行い、差分検証とコミットはClaude側で実施した。

  Distribution実験2自体（note側の導線追加）はこのタスクの対象外。
  moving-cost実験（2026-08-25終了予定）の結果レビュー後、別途開始する。
```
